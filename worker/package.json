{
  "name": "notes-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Background worker that polls Postgres item count",
  "main": "worker.js",
  "scripts": {
    "start": "node worker.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "pg": "^8.13.0"
  }
}
